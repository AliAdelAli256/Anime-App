import React from 'react';
import ReactTooltip from "react-tooltip";

const AddFavourite = () => {
	return (
		<>
      <ReactTooltip id="addTip" place="top" effect="solid">Add to favourites</ReactTooltip>
		</>
	);
};

export default AddFavourite;