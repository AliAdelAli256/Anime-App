import React from 'react';
import ReactTooltip from "react-tooltip";

const RemoveFavourites = () => {
	return (
		<>
			<ReactTooltip id="addTip" place="top" effect="solid">Remove from favourites</ReactTooltip>
		</>
	);
};

export default RemoveFavourites;